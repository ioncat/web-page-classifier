# Effort Log

Анализ времени, затраченного на разработку проекта.
Составлен на основе git-истории (дата/время коммитов).

---

## Активные дни

| День | Сессии (кластеры коммитов) | Что сделано | Оценка |
|------|---------------------------|-------------|--------|
| 7 мар | 11:28–17:10 | Начало проекта: SQLite, step1–3, Ollama-классификация, теги, compare, workers | ~6 ч |
| 8 мар | 14:21–20:36 | Батчинг, benchmark, --no-think, step2 workers, --stats, ML plan | ~6 ч |
| 9 мар | 20:19–23:03 | XLSX export, config refactor, error codes, User-Agent fix | ~3 ч |
| 12 мар | 13:22–19:45 | README переписан на русский, og:description в промпте, --no-description | ~5 ч |
| 21 мар | 21:45–23:19 | --refetch-description, JS challenge bypass, очистка БД | ~2 ч |
| 22 мар | 15:28–17:36 | Web UI: FastAPI + Tailwind, auth, шаблоны, удаление/перемещение URL, refetch | ~2.5 ч |
| 25 мар | 16:58–22:30 | Таксономия, domain rules, manual_override, --analytics, --defer-errors, парсер ответов, очистка проекта | ~5.5 ч |

| 13 апр | ~14:00–18:00 | Реорганизация репо (pipeline/, data/, docs/web/), обновление путей в коде, настройка CLAUDE.md для трёх проектов, глобальные правила | ~4 ч |
| 14 апр | ~10:00–14:00 | Стандартизация Claude для всех 5 проектов, обновление документации (пути README), bat/vbs лаунчеры | ~4 ч |

**Итого активных дней: 9 из 38 календарных (7 марта – 14 апреля 2026)**

---

## Разбивка по категориям

| Категория | Время |
|-----------|-------|
| Архитектура пайплайна (step1–3, db, config) | ~8–9 ч |
| LLM-классификация (промпты, таксономия, domain rules) | ~6–7 ч |
| Web UI (FastAPI, шаблоны, auth) | ~3–4 ч |
| Инструменты (benchmark, compare, analytics, export) | ~5–6 ч |
| Документация (README, backlog, ml-plan) | ~5–6 ч |
| Отладка, очистка БД, polish | ~3–4 ч |
| Инфраструктура (реорганизация репо, лаунчеры, настройка Claude) | ~8 ч |
| **Всего** | **~38–44 часов** |

---

## Сессия 25 марта 2026 (подробно)

**Продолжительность:** ~5.5 ч

| Задача | Описание |
|--------|----------|
| config/taxonomy.py | 5 разделов (L0), 31 категория (L1), единый источник для LLM и Web UI |
| config/domain_rules.py | 9 доменов, 3 типа правил (category / section / оба) |
| step3.py — domain rules | Разделение URL: domain match → без LLM, section → суженный промпт |
| step3.py — парсер ответов | Исправлен: модель отвечала многословно, парсер брал только первую строку |
| db.py — manual_override | Колонка + фильтр в get_done_unclassified() |
| db.py — deferred статус | defer_errors(), undefer_all(), отображение в --stats |
| db.py — get_analytics() | Полная аналитика: title/desc/category + топ доменов + deferred |
| main.py — --analytics | Человекочитаемая таблица с понятными подписями |
| main.py — --defer-errors / --undefer | Управление отложенными ошибками |
| main.py — UTF-8 fix | sys.stdout/stderr → UTF-8 для Windows |
| Web UI — sidebar с секциями | Категории сгруппированы по разделам таксономии |
| Web UI — модалка с секциями | Выбор категории с группировкой по разделам |
| Очистка БД | Google search (570+526), 404 (90), 410 (4), habr 403 (50), rozetka 403 (8) |
| Очистка корня | exports/, archive/, удалён links (3).txt |
| Классификация | 224 URL: 40 domain rules + 184 LLM → 207 OK, 57 ошибок → fix парсера → ~0 |
| Документация | README, backlog обновлены с domain rules, effort-log создан |

### Открытые вопросы
- Domain rules + section narrowing: habr.com содержит статьи вне IT (история, финансы). Section rule — жёсткое ограничение или подсказка?
- Интерактивный CLI: слишком много флагов, нужен удобный интерфейс (CLI меню / Web UI / гибрид)

---

## Примечания

- Разработка ведётся с активным использованием Claude Code.
- Перерывы между сессиями — работа параллельно с другими задачами.
- Эквивалент: **~4–4.5 полных рабочих дня** (по 8 часов).
